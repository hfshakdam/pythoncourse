#return statment = used in functions to send python values/ objects to the caller.
#       These values/ objects are known as the function's return value

def multiply(number1, number2):
    #result = number1 * number2
    #return result
    return number1 * number2 #shorter way of writing the above code



